{{#each page.pagedPosts}}
<article>
    <header>
        <span id="meta">{{dateFormat this.post.meta.date format='Do MMMM YYYY'}}</span>
        <a href="{{this.meta.slug}}.html">
            <h1>{{this.meta.title}}</h1>
        </a>
    </header>

    {{#if ../config.showFullContentOnIndex}}
    {{{this.content}}}
    {{else}}
    {{{this.meta.excerpt}}}
    {{/if}}


</article>
{{/each}}

{{>pagination page=page}}